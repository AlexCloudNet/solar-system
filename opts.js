// Solar system properties
const SUN_OPTS = {
    x: 400,
    y: 380,
    rad: 15,
}
let BIG_ORBIT_FLAG = 1;
let DELTA_ORBIT_RAD = 150;
let STARS_COUNT = 100;

export {
    SUN_OPTS,
    BIG_ORBIT_FLAG,
    DELTA_ORBIT_RAD,
    STARS_COUNT
};